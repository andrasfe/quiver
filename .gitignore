# Environment / secrets
.env
.env.*
!.env.example

# Python bytecode & build artefacts
__pycache__/
*.py[cod]
*$py.class
*.so
build/
dist/
*.egg-info/
*.egg
.eggs/
pip-wheel-metadata/

# Virtualenvs
.venv/
venv/
env/
.python-version

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.mypy_cache/
.ruff_cache/
.hypothesis/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs
*.log

# Beads (local workflow state)
.beads/

# Local-only design notes (kept off origin)
idea.md

# Hardware-experimentation notebooks — local only.
# The persistence module + generator script ARE pushed; the notebook that
# loads them and submits to IBM hardware is not (it carries API tokens
# and run-specific results we don't want public).
notebooks/
.ipynb_checkpoints/

# Hardware run results (per-experiment, may include device names etc.)
results/ibm_run_*.json
